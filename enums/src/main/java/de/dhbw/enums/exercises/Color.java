package de.dhbw.enums.exercises;

public enum Color {
    Red,
    Green,
    Blue,
    Yellow,
    Brown,
    Lila,
    Orange,
    Unknown;

    public Color mix(Color color) {

        // Hier: 'switch' anstelle von 'if-elseif-else' ...

        return Unknown;
    }

}